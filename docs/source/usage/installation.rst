Installation
============

Installation of the package is very straight forward using ``pip``:

.. code-block:: bash

    pip install ds-my-data

We used the name ``ds-my-data`` on PyPI to prevent name clashing with other projects that are named similar.